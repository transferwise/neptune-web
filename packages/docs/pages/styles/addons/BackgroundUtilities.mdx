export function BackgroundToken(props) {
  return (
    <div className="m-b-2">
      <div
        className={props.token}
        style={{
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          borderRadius: '3px',
          width: '160px',
          height: '48px',
        }}
      >
        .{props.token}
      </div>
    </div>
  );
}

import { DocLink } from '../../../utils/pageUtils';

Background utility classes.

**Note:** To use these classes in your application, you need `neptune.css` or `neptune-addons.css` on your page. Refer to <DocLink href="/about/Setup#installation">Installation</DocLink> on the Setup page to get started.

## Contextual backgrounds

<div className="m-b-4">
  <BackgroundToken token="bg-default" />
  <BackgroundToken token="bg-info" />
  <BackgroundToken token="bg-primary" />
  <BackgroundToken token="bg-success" />
  <BackgroundToken token="bg-danger" />
  <BackgroundToken token="bg-warning" />
</div>

export const meta = {
  name: 'Background utilities',
};
