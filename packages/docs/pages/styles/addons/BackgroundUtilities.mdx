export function BackgroundToken(props) {
  return (
    <div className="m-b-2">
      <div
        className={props.token}
        style={{
          backgroundColor: `var(${props.token})`,
          display: 'flex',
          justifyContent: 'center',
          alignItems: 'center',
          borderRadius: '3px',
          width: '160px',
          height: '48px',
        }}
      >
        .{props.token}
      </div>
    </div>
  );
}

**Note:** To use these classes in your application, you need `neptune-addons.css` on your page. Refer to [Installing CSS](/about/Setup#installing-css) on the Setup page to get started.

## Contextual backgrounds

Neptune uses utility background classes.

<div className="m-b-4">
  <BackgroundToken token="bg-default" />
  <BackgroundToken token="bg-info" />
  <BackgroundToken token="bg-primary" />
  <BackgroundToken token="bg-success" />
  <BackgroundToken token="bg-danger" />
  <BackgroundToken token="bg-warning" />
</div>

<p>
  <code>@color-base-white</code> for <code>bg-info</code> and <code>bg-primary</code> headings.
</p>

export const meta = {
  name: 'Background Utilities',
};

;
