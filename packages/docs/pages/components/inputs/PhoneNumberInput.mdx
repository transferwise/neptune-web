import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { PhoneNumberInput } from '@transferwise/components';
import code from '../../../liveEditorCode/phoneNumberInput.code';

<LiveEditorBlock code={code} scope={{ PhoneNumberInput }} />
<GeneratePropsTable componentName="PhoneNumberInput" />

export const meta = {
  name: 'PhoneNumberInput',
};
