import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Button } from '@transferwise/components';
import code from '../../../liveEditorCode/button.code';

<LiveEditorBlock code={code} scope={{ Button }} />
<GeneratePropsTable componentName="Button" />

## Variants

<div className="scroll-table">
  <table className="docs-table table ">
    <thead>
      <tr>
        <th>Type</th>
        <th>Priority</th>
        <th>Preview</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th>Accent</th>
        <th>Primary</th>
        <td>
          <Button block={true} onClick={() => {}}>
            Continue
          </Button>
        </td>
      </tr>
      <tr>
        <th>Accent</th>
        <th>Secondary</th>
        <td>
          <Button priority="secondary" block={true} onClick={() => {}}>
            Convert money
          </Button>
        </td>
      </tr>
      <tr>
        <th>Accent</th>
        <th>Tertiary</th>
        <td>
          <Button priority="tertiary" block={true} onClick={() => {}}>
            Convert money
          </Button>
        </td>
      </tr>
      <tr>
        <th>Positive</th>
        <th>Primary</th>
        <td>
          <Button type="positive" block={true} onClick={() => {}}>
            Send money
          </Button>
        </td>
      </tr>
      <tr>
        <th>Positive</th>
        <th>Secondary</th>
        <td>
          <Button type="positive" priority="secondary" block={true} onClick={() => {}}>
            Confirm
          </Button>
        </td>
      </tr>
      <tr>
        <th>Negative</th>
        <th>Primary</th>
        <td>
          <Button type="negative" block={true} onClick={() => {}}>
            Delete
          </Button>
        </td>
      </tr> <tr>
        <th>Negative</th>
        <th>Secondary</th>
        <td>
          <Button type="negative" priority="secondary" block={true} onClick={() => {}}>
            Cancel
          </Button>
        </td>
      </tr>
    </tbody>
  </table>
</div>

export const meta = {
  name: 'Button',
};
