import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Button } from '@transferwise/components';
import code from '../../../liveEditorCode/button.code';

Simple wrapper component for all button types and their states.

<LiveEditorBlock code={code} scope={{ Button }} />
<GeneratePropsTable componentName="Button" />

## Types

<div className="scroll-table">
  <table className="docs-table table ">
    <thead>
      <tr>
        <th>Name</th>
        <th>Preview</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th scope="row">Primary</th>
        <td>
          <Button size="md" type="primary" block={true}>
            Continue
          </Button>
        </td>
      </tr>
      <tr>
        <th scope="row">Pay</th>
        <td>
          <Button size="md" type="pay" block={true}>
            Send money
          </Button>
        </td>
      </tr>
      <tr>
        <th scope="row">Secondary</th>
        <td>
          <Button size="md" type="secondary" block={true}>
            Manage my card
          </Button>
        </td>
      </tr>{' '}
      <tr>
        <th scope="row">Danger</th>
        <td>
          <Button size="md" type="danger" block={true}>
            Delete recipient
          </Button>
        </td>
      </tr>
      <tr>
        <th scope="row">Link</th>
        <td>
          <Button size="md" type="link" block={true}>
            Get help
          </Button>
        </td>
      </tr>
    </tbody>
  </table>
</div>

export const meta = {
  name: 'Button',
};
