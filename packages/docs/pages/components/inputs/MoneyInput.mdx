import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { MoneyInput } from '@transferwise/components';
import code from '../../../liveEditorCode/moneyInput.code';

<LiveEditorBlock code={code} scope={{ MoneyInput }} display="vertical" />
<GeneratePropsTable componentName="MoneyInput" />

export const meta = {
  name: 'MoneyInput',
};
