import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { DateInput } from '@transferwise/components';
import code from '../../../liveEditorCode/dateInput.code';

<LiveEditorBlock code={code} scope={{ DateInput }} />
<GeneratePropsTable componentName="DateInput" />

export const meta = {
  name: 'Date Input',
};
