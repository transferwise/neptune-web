import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Select } from '@transferwise/components';
import code from '../../../liveEditorCode/select.code';
import { Profile as ProfileIcon, Globe as GlobeIcon } from '@transferwise/icons';

<LiveEditorBlock code={code} scope={{ Select, GlobeIcon, ProfileIcon }} display="vertical" />
<GeneratePropsTable componentName="Select" />

export const meta = {
  name: 'Select',
};
