import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { FormControl } from '@transferwise/dynamic-flows';
import code from '../../../liveEditorCode/formcontrol.code';

**Note:** This component is deprecated. It can now be found in `@transferwise/dynamic-flows`.

<LiveEditorBlock code={code} scope={{ FormControl }} />
<GeneratePropsTable componentName="FormControl" />

export const meta = {
  name: 'FormControl',
};
