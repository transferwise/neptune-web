import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { JsonSchemaForm } from '@transferwise/dynamic-flows';
import code from '../../../liveEditorCode/jsonSchemaForm.code';

**Note:** This component is in `@transferwise/dynamic-flows`. To use, install this package and import from `@transferwise/dynamic-flows`.
This component requires react-intl provider with translation messages provided in `./build/i18n`. See example [here](https://transferwise.github.io/neptune-web/about/Setup#usage).

<LiveEditorBlock code={code} scope={{ JsonSchemaForm }} />
<GeneratePropsTable componentName="JsonSchemaForm" />

export const meta = {
  name: 'JsonSchemaForm',
};
