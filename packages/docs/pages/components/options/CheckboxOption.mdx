import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { FastFlag as FastFlagIcon } from '@transferwise/icons';
import { CheckboxOption } from '@transferwise/components';
import code from '../../../liveEditorCode/checkboxOption.code';

<LiveEditorBlock code={code} scope={{ CheckboxOption, FastFlagIcon }} />
<GeneratePropsTable componentName="CheckboxOption" />

export const meta = {
  name: 'CheckboxOption',
};
