import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import Flex from '@transferwise/components/src/flex';
import Box from '@transferwise/components/src/box';
import '../../static/assets/box.docs.css';
import '@transferwise/neptune-css/dist/css/flex.css';
import code from '../../liveEditorCode/flexbox.code';

<div id="box">
  <LiveEditorBlock code={code} scope={{ Flex, Box }} />
  <GeneratePropsTable componentName="Flex" />
</div>

## Flex

Flex offers a display flex container for Box. It also offers the possibility to switch direction based on breakpoints.
Here an example of swithcing direction:
`direction={xs:'row',sm:'row-reverse',md:'column',lg:'column-reverse','xl:'column'}`.

Flex also offers the possibility to manage the margin and the paddings of its boxes.
Flex and box shouldn't be used to directly to create page structures but they should be use to create layout components that will be used to create pages structures. In this way it will be easier to lock down all the different designs that we use.

## Box

Box component is a simple html container that works inside display:flex parent
The most important functionality of Box is the ability of changing its size based on breakpoint.
It is possible to pass any size (width or height depending on the flow direction) desired between 0 and ∞.
Values between 0 and 1 will be interpreted as percentages, values > 1 will be interpreted as pixel values.

Example `{'size={xs:0, sm:1/2, md:1/4, lg:200, xl:300}'}`

<table className="table">
  <thead>
    <tr>
      <th>Flex</th>
      <th>Display</th>
      <th>Breakpoint</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>0</td>
      <td>none</td>
      <td>xs</td>
    </tr>
    <tr>
      <td>0 1 50%</td>
      <td>flex</td>
      <td>sm</td>
    </tr>
    <tr>
      <td>0 1 25%</td>
      <td>flex</td>
      <td>md</td>
    </tr>
    <tr>
      <td>0 0 200px</td>
      <td>flex</td>
      <td>lg</td>
    </tr>
    <tr>
      <td>0 0 300px</td>
      <td>flex</td>
      <td>xl</td>
    </tr>
  </tbody>
</table>

export const meta = {
  name: 'Flex & Box',
};
