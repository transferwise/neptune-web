import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Stepper } from '@transferwise/components';
import code from '../../liveEditorCode/stepper.code';

<LiveEditorBlock code={code} scope={{ Stepper }} />
<GeneratePropsTable componentName="Stepper" />

export const meta = {
  name: 'Stepper',
};

;
