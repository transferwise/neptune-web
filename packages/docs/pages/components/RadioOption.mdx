import { RenderTabs, LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { RadioOption } from '@transferwise/components';
import FastFlagIcon from '@transferwise/icons/react/fast-flag';
import code from '../../liveEditorCode/radioOption.code';

<RenderTabs>
#web
<>
  <LiveEditorBlock code={code} scope={{ RadioOption,FastFlagIcon }} />
  <GeneratePropsTable componentName="RadioOption" />
</>
#ios
</RenderTabs>

export const meta = {
  abstractURL: '',
  name: 'Radio Option',
  size: '',
  isPlaceholder: true,
};