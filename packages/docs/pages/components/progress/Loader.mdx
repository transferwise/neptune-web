import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Loader } from '@transferwise/components';
import code from '../../../liveEditorCode/loader.code';

<LiveEditorBlock code={code} scope={{ Loader }} />
<GeneratePropsTable componentName="Loader" />

export const meta = {
  name: 'Loader',
};

;
