import { RenderTabs, LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Select } from '@transferwise/components';
import code from '../../liveEditorCode/select.code';

<RenderTabs>
#web
<>
  <LiveEditorBlock code={code} scope={{ Select }} />
  <GeneratePropsTable componentName="Select" />
</>
#ios
</RenderTabs>

export const meta = {
  abstractURL: '',
  name: 'Select',
  size: '',
  isPlaceholder: true,
};