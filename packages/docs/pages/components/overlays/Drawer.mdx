import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Drawer, Button } from '@transferwise/components';
import code from '../../../liveEditorCode/drawer.code';

**Note:** To use this component you must include `neptune.css` or `neptune-addons.css` in your application.

<LiveEditorBlock code={code} scope={{ Drawer, Button }} />
<GeneratePropsTable componentName="Drawer" />

export const meta = {
  name: 'Drawer',
};
