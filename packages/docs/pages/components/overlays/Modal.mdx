import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Modal, Button, DateInput } from '@transferwise/components';
import code from '../../../liveEditorCode/modal.code';

<LiveEditorBlock code={code} scope={{ Modal, Button, DateInput }} />
<GeneratePropsTable componentName="Modal" />

export const meta = {
  name: 'Modal',
};
