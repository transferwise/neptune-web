import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Dimmer, Button } from '@transferwise/components';
import code from '../../../liveEditorCode/dimmer.code';

<LiveEditorBlock code={code} scope={{ Dimmer, Button }} />
<GeneratePropsTable componentName="Dimmer" />

export const meta = {
  name: 'Dimmer',
};
