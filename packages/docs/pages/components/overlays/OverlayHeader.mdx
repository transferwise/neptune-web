import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { OverlayHeader, Avatar, Logo } from '@transferwise/components';
import { Briefcase as BriefcaseIcon, Profile as ProfileIcon } from '@transferwise/icons';
import code from '../../../liveEditorCode/overlayHeader.code';

Simple header component that displays a responsive logo an avatar and a close button.

<LiveEditorBlock
  code={code}
  scope={{ OverlayHeader, Avatar, BriefcaseIcon, ProfileIcon, Logo }}
  display="vertical"
/>
<GeneratePropsTable componentName="OverlayHeader" />

export const meta = {
  name: 'OverlayHeader',
};
