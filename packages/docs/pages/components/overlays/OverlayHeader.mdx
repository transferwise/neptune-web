import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { OverlayHeader } from '@transferwise/components';
import { Cross as CrossIcon, Check as CheckIcon } from '@transferwise/icons';
import code from '../../../liveEditorCode/overlayHeader.code';

Simple header component that displays a responsive logo an avatar and a close button.

<LiveEditorBlock code={code} scope={{ OverlayHeader }} display="vertical" />
<GeneratePropsTable componentName="OverlayHeader" />

## Usage

Some elements can be toggled based on props values as per the table below.

<div className="scroll-table">
  <table className="docs-table table">
    <thead>
      <tr>
        <th>Prop</th>
        <th>Logo</th>
        <th>closeButton</th>
        <th>avatar</th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <th scope="row">closeButton = null</th>
        <td className="text-center">
          <CheckIcon />
        </td>
        <td>
          <CrossIcon />
        </td>
        <td>
          <CrossIcon />
        </td>
      </tr>
      <tr>
        <th scope="row">avatar = null</th>
        <td>
          <CheckIcon />
        </td>
        <td>
          <CheckIcon />
        </td>
        <td>
          <CrossIcon />
        </td>
      </tr>
    </tbody>
  </table>
</div>

export const meta = {
  name: 'OverlayHeader',
};
