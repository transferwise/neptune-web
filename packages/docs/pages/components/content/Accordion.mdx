import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Accordion } from '@transferwise/components';
import code from '../../../liveEditorCode/accordion.code';

<LiveEditorBlock code={code} scope={{ Accordion }} />
<GeneratePropsTable componentName="Accordion" />

export const meta = {
  name: 'Accordion',
};
