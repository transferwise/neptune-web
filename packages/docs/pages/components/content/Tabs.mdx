import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Tabs, Alert } from '@transferwise/components';
import code from '../../../liveEditorCode/tabs.code';

<LiveEditorBlock code={code} scope={{ Tabs, Alert }} display="vertical" />
<GeneratePropsTable componentName="Tabs" />

export const meta = {
  name: 'Tabs',
};
