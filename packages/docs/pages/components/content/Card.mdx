import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Card } from '@transferwise/components';
import { FastFlag as FastFlagIcon } from '@transferwise/icons';
import code from '../../../liveEditorCode/card.code';

<LiveEditorBlock code={code} scope={{ Card, FastFlagIcon }} />
<GeneratePropsTable componentName="Card" />

## Group of cards

Cards rendered as `li` elements in a [panel-list](https://bootstrap.transferwise.com/components/#panels-list-group) with `list-group panel-list-group list-group-inactive` class.

<ul className="list-group panel-list-group list-group-inactive">
  <Card
    as="li"
    title="Collapsed card"
    details="details"
    isExpanded={false}
    onClick={() => alert('toggled')}
    icon={<FastFlagIcon />}
  >
    <div />
  </Card>
  <Card
    as="li"
    title="Expanded card"
    details="details"
    isExpanded={true}
    onClick={() => alert('toggled')}
    icon={<FastFlagIcon />}
  >
    I am an expanded card.
  </Card>
  <Card as="li" title="Empty card" details="I do not have children" icon={<FastFlagIcon />} />
  <Card
    as="li"
    title="Collapsed card"
    details="details"
    isExpanded={false}
    onClick={() => alert('toggled')}
    icon={<FastFlagIcon />}
  >
    <div />
  </Card>
</ul>

export const meta = {
  name: 'Card',
};
