import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Radio } from '@transferwise/components';
import code from '../../../liveEditorCode/radio.code';

This component is used inside other components, such as RadioGroup and RadioOption. Please do not use it directly.

<LiveEditorBlock code={code} scope={{ Radio }} />
<GeneratePropsTable componentName="Radio" />

export const meta = {
  name: 'Radio',
};
