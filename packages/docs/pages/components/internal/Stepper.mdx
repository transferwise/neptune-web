import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Stepper } from '@transferwise/components';
import code from '../../../liveEditorCode/stepper.code';

This component is used inside other components, such as FlowNavigation. Please do not use it directly.

<LiveEditorBlock code={code} scope={{ Stepper }} display="vertical" />
<GeneratePropsTable componentName="Stepper" />

export const meta = {
  name: 'Stepper',
};
