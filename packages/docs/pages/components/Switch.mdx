import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Switch } from '@transferwise/components';
import code from '../../liveEditorCode/switch.code';

<LiveEditorBlock code={code} scope={{ Switch }} />
<GeneratePropsTable componentName="Switch" />

export const meta = {
  name: 'Switch',
};
