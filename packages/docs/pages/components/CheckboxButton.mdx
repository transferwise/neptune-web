import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { CheckboxButton } from '@transferwise/components';
import code from '../../liveEditorCode/checkboxButton.code';

<LiveEditorBlock code={code} scope={{ CheckboxButton }} />
<GeneratePropsTable componentName="CheckboxButton" />

export const meta = {
  name: 'CheckboxButton',
};
