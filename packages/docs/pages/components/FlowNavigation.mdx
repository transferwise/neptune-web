import { RenderTabs, LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { FlowNavigation } from '@transferwise/components';
import code from '../../liveEditorCode/flowNavigation.code';

<RenderTabs>
#web
<>
  <LiveEditorBlock code={code} scope={{ FlowNavigation }} display='vertical'/>
  <GeneratePropsTable componentName="FlowNavigation" />
</>
#ios
</RenderTabs>

export const meta = {
  abstractURL: '',
  name: 'Flow Navigation',
  size: '',
  isPlaceholder: true,
};
