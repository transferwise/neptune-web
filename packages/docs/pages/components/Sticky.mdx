import { RenderTabs, LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Sticky,Button } from '@transferwise/components';
import code from '../../liveEditorCode/sticky.code';

<>
  <LiveEditorBlock code={code} scope={{ Sticky, Button }} />
  <GeneratePropsTable componentName="Sticky" />
</>

export const meta = {
  abstractURL: '',
  name: 'Sticky',
  size: '',
  isPlaceholder: true,
};

;
