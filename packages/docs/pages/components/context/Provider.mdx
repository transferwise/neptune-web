import { FormattedMessage } from 'react-intl';
import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Provider, Select } from '@transferwise/components';
import supportedLanguages from '@transferwise/components/build/i18n';

import messages from '@transferwise/components/build/es/no-polyfill/common/closeButton/CloseButton.messages';
import code from '../../../liveEditorCode/provider.code';

export const defaultLocale = 'en-GB';
export const localeOptions = Object.keys(supportedLanguages).map((lang) => ({
  value: lang,
  label: lang,
}));

Provides contextual data such as translations to components. See the [usage guide](https://transferwise.github.io/neptune-web/branch/i18y/about/Setup#usage) for more.

<LiveEditorBlock
  code={code}
  scope={{
    Provider,
    Select,
    FormattedMessage,
    messages,
    supportedLanguages,
    localeOptions,
    defaultLocale,
  }}
/>
<GeneratePropsTable componentName="Provider" />

export const meta = {
  name: 'Provider',
};
