import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Tooltip, Button } from '@transferwise/components';
import code from '../../../liveEditorCode/tooltip.code';

<LiveEditorBlock code={code} scope={{ Tooltip, Button }} />
<GeneratePropsTable componentName="Tooltip" />

export const meta = {
  name: 'Tooltip',
};
