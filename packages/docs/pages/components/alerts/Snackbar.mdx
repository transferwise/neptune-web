import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { SnackbarProvider, SnackbarConsumer, useSnackbar, Button } from '@transferwise/components';
import '@transferwise/neptune-css/dist/css/snackbar.css';
import code from '../../../liveEditorCode/snackbar.code';

<LiveEditorBlock code={code} scope={{ Button, SnackbarConsumer, SnackbarProvider }} />
<GeneratePropsTable componentName="Snackbar" />

export const meta = {
  name: 'Snackbar',
};

;
