import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Button, Popover } from '@transferwise/components';
import code from '../../../liveEditorCode/popover.code';

<LiveEditorBlock code={code} scope={{ Popover, Button }} />
<GeneratePropsTable componentName="Popover" />

export const meta = {
  name: 'Popover',
};
