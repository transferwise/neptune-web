import { LiveEditorBlock, GeneratePropsTable } from '../../../utils';
import { Nudge, Image } from '@transferwise/components';
import giftImage from  '../../../static/assets/img/gift.png';
import code from '../../../liveEditorCode/nudge.code';

<LiveEditorBlock code={code} scope={{ Nudge, Image, giftImage }} />
<GeneratePropsTable componentName="Nudge" />

export const meta = {
  name: 'Nudge',
};
