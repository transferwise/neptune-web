import { RenderTabs, LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Chip } from '@transferwise/components';
import code from '../../liveEditorCode/chip.code';

<RenderTabs>
#web
<>
  <LiveEditorBlock code={code} scope={{ Chip }} />
  <GeneratePropsTable componentName="Chip" />
</>
#ios
</RenderTabs>

export const meta = {
  abstractURL: '',
  name: 'Chip',
  size: '',
  isPlaceholder: true,
};
