import { RenderTabs, LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { Money } from '@transferwise/components';
import code from '../../liveEditorCode/money.code';

<RenderTabs>
#web
<>
  <LiveEditorBlock code={code} scope={{ Money }} />
  <GeneratePropsTable componentName="Money" />
</>
#ios
</RenderTabs>

export const meta = {
  abstractURL: '',
  name: 'Money',
  size: '',
  isPlaceholder: true,
};
