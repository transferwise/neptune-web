import { LiveEditorBlock, GeneratePropsTable } from '../../utils';
import { MobileNav } from '@transferwise/components/build/es/polyfill/lab';

import code from '../../liveEditorCode/mobileNav.code';

<LiveEditorBlock code={code} scope={{ MobileNav }} />
<GeneratePropsTable componentName="MobileNav" />

export const meta = {
  name: `MobileNav`,
};
