const code = `<DateLookup
  value={null}
  min={null }
  max={null }
  size={DateLookup.Sizes.MEDIUM}
  locale={'en-GB'}
  placeholder={'I m a placeholder'}
  label={'label'}
  monthFormat={'long'}
  disabled={false}
  onChange={(v)=> alert(v)}
/>`;

export default code;
