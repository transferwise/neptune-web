// This is here to clean up the pathing, we'll keep the type validators in common
import {
  isString,
  isNumber,
  isInteger,
  isBoolean,
  isArray,
  isObject,
  isNull,
  isUndefined,
} from '../../../common/validation/type-validators';

export { isString, isNumber, isInteger, isBoolean, isObject, isArray, isNull, isUndefined };
