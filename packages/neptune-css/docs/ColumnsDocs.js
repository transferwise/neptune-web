import React from "react";

const Columns = () => (
  <section id="columns" className="wrap--content section">
    <h1 className="colored-dot">Column system</h1>
  </section>
    );

export default Columns;
