import React from 'react';

const Columns = () => (
  <section id="columns" className="container--wide section">
    <h1 className="colored-dot doc-section__title">Column system</h1>
  </section>
);

export default Columns;
