import React from 'react';

const Columns = () => (
  <section id="columns" className="container--wide section">
    <h1 className="colored-dot">Column system</h1>
  </section>
);

export default Columns;
