import { defineMessages } from 'react-intl';

export default defineMessages({
  ariaLabel: {
    id: 'neptune.CloseButton.ariaLabel',
    defaultMessage: 'Close',
    description: 'For `aria-label` of root button element of the component',
  },
});
